import React from "react";
import { StyledInput, StyledSection } from "./styles";

const SearchBar = () => {
  return (
    <StyledSection>
      <StyledInput />
    </StyledSection>
  );
};

export default SearchBar;
